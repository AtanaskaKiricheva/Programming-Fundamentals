using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;

public class Example
{
    public static void Main()
    {
        int n = int.Parse(Console.ReadLine());
        // key - legionName => soldierType - soldier count
        var legions = new Dictionary<string, Dictionary<string, long>>();

        // key - legionName => activity
        var activities = new Dictionary<string, long>();

        for (int i = 0; i < n; i++)
        {
            string[] input = Console.ReadLine().Split(' ','-','>','=',':');
            long lastActivity = long.Parse(input[0]);
            string legionName = input[1];
            string soldierType = input[2];
            long soldierCount = long.Parse(input[3]);





            //case 1 ->
            var sortedA = activities
            .Where(a => a.Value < givenActivity);

            legions.Where(l => activities
            .Where(a => a.Value < givenActivity)
            .ContainsValue(l))
            .OrderedByDescending(s => s);

            //legions
            // Beatels -> Soldier - 2000
            // Red_Ones -> Workers - 20 0000
            
            // activities
            // Beatels -> 1;
            // rED_oNES - 2;


            
            

            
        }
















    }
}
